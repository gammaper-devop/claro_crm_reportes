/*
import { EncryptionManager } from './encryption.manager';

const public_key = Math.log(1000).toString();
const valueToEncrypt = 'String to be encrypted';
console.log('public_key: ', public_key);

const myEncryptor = new EncryptionManager(public_key);

const hash = myEncryptor.getEncryptor('browser').encrypt(valueToEncrypt);
console.log('hash: ', hash);
console.log('valor luego de ser desencriptado: ', myEncryptor.getEncryptor('browser').unEncrypt(hash));
*/
