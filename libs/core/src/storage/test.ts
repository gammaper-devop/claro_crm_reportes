/*
import { EStorageType, IStorageConfig, StorageFactory } from './';

const config: IStorageConfig = {
  storageType: EStorageType.LOCAL,
  secretKey: String(Math.log(1000)),
  i18nLang: 'en_US',
}

const provider = StorageFactory(config);

console.log(1, provider.get('user'));

provider.set('user', { user: 'ECNF' });
console.log(2, provider.get('user'));

provider.remove('user');
console.log(3, provider.get('user'));

provider.set('user', { user: 'ECNF' });
console.log(4, provider.get('user'));

provider.clear();
console.log(5, provider.get('user'));
*/
