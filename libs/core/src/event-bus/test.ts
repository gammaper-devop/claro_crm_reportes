/* import { EventBus } from './event-bus';

const eventBus = EventBus.getInstance();

eventBus.$on('openModal', (data: any) => {
  console.log(data);
});

const eventBus2 = EventBus.getInstance();

eventBus2.$emit('openModal', {});
 */
