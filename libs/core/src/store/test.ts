/* import { Store } from './state';

export class UserService extends Store<{}> {
  create(user: any) {
    this.setState(user);
  }

  update(user: any) {
    const newState = { ...this.getValue(), ...{ user } };
    this.setState(newState);
  }
}
 */
